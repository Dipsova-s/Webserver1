@model List<EveryAngle.Core.ViewModels.Model.ModelViewModel>
@for (int i = 0; i < Model.Count; i++)
{
    Html.RenderAction("GetModelOverview", "Model", new { modelUri = Model[i].Uri.ToString(), licensesData = ViewBag.LicensesData, modelServersUri = ViewBag.ModelServersUri });
}