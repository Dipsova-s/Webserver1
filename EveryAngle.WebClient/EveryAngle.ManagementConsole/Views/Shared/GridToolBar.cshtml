@using EveryAngle.ManagementConsole.Models
@model List<PageToolbarButtonModel>

@{
    if (Model != null)
    {
        foreach (PageToolbarButtonModel button in Model)
        {
            if (button.Type == PageToolbarButtonType.GridEditDelete)
            {
                  @Html.Raw(button.Template);
            }

        }
    }      
}


